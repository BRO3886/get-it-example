import 'package:flutter/material.dart';

import '../service.dart';
import '../service_locator.dart';

class MyCustomWidget1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('custom widget one'),
    );
  }
}
